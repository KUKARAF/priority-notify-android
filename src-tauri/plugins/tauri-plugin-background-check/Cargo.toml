[package]
name = "tauri-plugin-background-check"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-background-check"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
tauri = "2"
serde = { version = "1", features = ["derive"] }
